<UserControl x:Class="Locomotiv.View.TrainManagementWindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Locomotiv.View"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Gestion des trains"
                       FontSize="24"
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>

            <Button Content="✕"
                    Command="{Binding CloseCommand}"
                    Width="30"
                    Height="30"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    FontSize="16"
                    FontWeight="Bold"
                    Background="#E0E0E0"
                    BorderBrush="#BDBDBD"
                    Cursor="Hand"
                    ToolTip="Retour à la carte"/>

            <Button
         Content="↓ Creer un nouveau Train ↓"
         Command="{Binding NavigateToCreateTrainForStationViewCommand}"
         Padding="10"
         Background="Black"
         Foreground="White"
         FontWeight="Bold"
         HorizontalAlignment='Left' Margin="350,0,0,0" VerticalAlignment="Top" Width="200"/>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Trains disponibles"
                           FontSize="18"
                           FontWeight="Bold"
                           Margin="0,0,0,10"
                           Grid.Row="0"/>

                <ListBox Grid.Row="1"
                         ItemsSource="{Binding AvailableTrains}"
                         SelectedItem="{Binding SelectedAvailableTrain}"
                         Margin="0,0,0,10">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="5">
                                <TextBlock Text="{Binding TypeOfTrain, StringFormat='Type: {0}'}"
                                           FontWeight="Bold"/>
                                <TextBlock Text="{Binding State, StringFormat='État: {0}'}"/>
                                <TextBlock Text="{Binding PriotityLevel, StringFormat='Priorité: {0}'}"/>
                                <TextBlock>
                                    <Run Text="Locomotives: "/>
                                    <Run Text="{Binding Locomotives.Count, Mode=OneWay}"/>
                                </TextBlock>
                                <TextBlock>
                                    <Run Text="Wagons: "/>
                                    <Run Text="{Binding Wagons.Count, Mode=OneWay}"/>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Button Grid.Row="2"
                        Content="Ajouter le train sélectionné →"
                        Command="{Binding AddTrainCommand}"
                        Padding="10"
                        Background="Green"
                        Foreground="White"
                        FontWeight="Bold"
                        HorizontalAlignment="Stretch"
                        Margin="0,0,0,5"/>

                <Button Grid.Row="3"
                        Content="Supprimer définitivement le train"
                        Command="{Binding DeleteAvailableTrainCommand}"
                        Padding="10"
                        Background="#D32F2F"
                        Foreground="White"
                        FontWeight="Bold"
                        HorizontalAlignment="Stretch"/>
            </Grid>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,10">
                    <TextBlock Text="Trains à la station"
                               FontSize="18"
                               FontWeight="Bold"/>
                    <TextBlock Text="{Binding CapacityText, StringFormat='Capacité: {0}'}"
                               FontSize="14"
                               Foreground="Gray"
                               Margin="0,5,0,0"/>
                </StackPanel>

                <ListBox Grid.Row="1"
                         ItemsSource="{Binding Trains}"
                         SelectedItem="{Binding SelectedTrain}"
                         Margin="0,0,0,10">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="5">
                                <TextBlock Text="{Binding TypeOfTrain, StringFormat='Type: {0}'}"
                                           FontWeight="Bold"/>
                                <TextBlock Text="{Binding State, StringFormat='État: {0}'}"/>
                                <TextBlock Text="{Binding PriotityLevel, StringFormat='Priorité: {0}'}"/>
                                <TextBlock>
                                    <Run Text="Locomotives: "/>
                                    <Run Text="{Binding Locomotives.Count, Mode=OneWay}"/>
                                </TextBlock>
                                <TextBlock>
                                    <Run Text="Wagons: "/>
                                    <Run Text="{Binding Wagons.Count, Mode=OneWay}"/>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
               
                <Button Grid.Row="2"
                        Content="← Enlever le train sélectionné"
                        Command="{Binding DeleteTrainCommand}"
                        Padding="10"
                        Background="#D32F2F"
                        Foreground="White"
                        FontWeight="Bold"
                        HorizontalAlignment="Stretch"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
